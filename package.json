{
  "name": "quireink-wordpress-theme",
  "private": true,
  "type": "module",
  "scripts": {
    "extract": "bun tools/extract.ts",
    "check:all": "bun run check:filesize && bun run check:order && bun run check:bridge && bun run check:contrast && bun run check:classes && bun run check:escape && bun run check:generated && bun run check:headers && bun run check:docs",
    "check:filesize": "bun tools/checks/file-size.ts",
    "check:order": "bun tools/checks/stylesheet-order.ts",
    "check:bridge": "bun tools/checks/bridge-tokens.ts",
    "check:escape": "bun tools/checks/php-escape.ts",
    "check:generated": "bun tools/checks/generated-in-sync.ts",
    "check:docs": "bun tools/checks/docs.ts",
    "pot": "docker compose -f dev/docker-compose.yml exec -T cli wp --path=/var/www/html i18n make-pot wp-content/themes/quire-ink wp-content/themes/quire-ink/languages/quire-ink.pot --domain=quire-ink --exclude=node_modules",
    "check:contrast": "bun tools/checks/contrast.ts",
    "check:classes": "bun tools/checks/dead-classes.ts",
    "check:headers": "bun tools/checks/headers.ts"
  }
}
